---
description: Sync Angel AI operational markdown after a meaningful implementation slice so the handoff, backlog, and patch history stay aligned with verified code.
---

# /sync-docs - Sync Operational Docs

$ARGUMENTS

## Task

Use the `sync-docs` skill at `.agent/skills/sync-docs/SKILL.md`.

## Goal

Update every relevant active markdown file after implementation and verification without drifting into archive-only material or unrelated docs churn.

## Required Outcome

Return a short doc-sync summary with:

1. the behavior that changed
2. markdown files updated
3. verification actually run
4. remaining docs intentionally left untouched
5. the next session's immediate action
