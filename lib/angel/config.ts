export const angelRecommendationPills = [
  '18+ only',
  'Communications, not Dating',
  'Friend-first, slow romance',
  'Next-day continuity paywall',
  'Astral as a hidden engine',
  'Voice notes plus bounded Angel voice',
  'OpenClaw as a sidecar runtime',
] as const

export const angelImplementationRecommendations = [
  'Keep onboarding chat-shaped even when required data is being collected.',
  'Store structured facts first, then regenerate user.md and soul.md from that source of truth.',
  'Keep Angel voice opt-in, quota-bounded, and clearly AI-generated rather than trying to turn the app into an always-on voice assistant.',
  'Hold romance behind user-led warmth and consistent consent signals rather than a mode toggle.',
] as const
